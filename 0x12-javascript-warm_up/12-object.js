#!/usr/bin/node

// Updating the code to change a value within an object
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);

// MY CODE HERE
myObject.value = 89;

console.log(myObject);
